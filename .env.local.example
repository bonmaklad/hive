# Copy to .env.local and fill in values, then restart dev server

# Public webhook for the contact form (Power Automate -> When an HTTP request is received)
# This is exposed to the browser, so only use anonymous webhook URLs intended for public use.
NEXT_PUBLIC_CONTACT_WEBHOOK_URL=

# Optional: If you later decide to use an email provider fallback via server route, you can add keys here.
# RESEND_API_KEY=
# RESEND_FROM=HIVE HQ <no-reply@hivehq.nz>

# Supabase (Platform UI)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional: public origin override for auth redirects behind proxies (e.g. https://hivehq.nz)
NEXT_PUBLIC_SITE_URL=

# Server-only: required for admin onboarding scripts (never expose to the browser)
# Use either name (SUPABASE_SERVICE_KEY is common in self-hosted setups)
# SUPABASE_SERVICE_ROLE_KEY=
# SUPABASE_SERVICE_KEY=
# Optional: server-only Supabase URL override (otherwise uses NEXT_PUBLIC_SUPABASE_URL)
# SUPABASE_URL=
# Optional: server-only canonical site URL for invites (otherwise uses NEXT_PUBLIC_SITE_URL)
# SITE_URL=

# Stripe (pay-as-you-go room bookings)
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
